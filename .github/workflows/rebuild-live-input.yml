name: Rebuild Live Voice input transport

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  rebuild-live-input:
    if: "!contains(github.event.head_commit.message, 'Rebuild Live Voice input transport applied')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Replace manual VAD with Gemini automatic VAD
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          p = Path('live_voice.py')
          s = p.read_text(encoding='utf-8')

          start