name: Rebuild Live Voice input transport

on: push

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Patch live_voice.py
        run: |
          python - <<'PY'
          from pathlib import Path

          p = Path("live_voice.py")
          s = p.read_text(encoding="utf-8")
          start_marker = "# ============================================================\n# MICROPHONE: MANUAL VAD"
          end_marker = "# ============================================================\n# RECEIVE LOOP"
          start = s.find(start_marker)
          end = s.find(end_marker, start)
          if start < 0 or end < 0:
              raise SystemExit("Manual VAD block not found; refusing to modify live_voice.py")

          microphone = '''# ============================================================
# MICROPHONE: GEMINI AUTOMATIC VAD
# ============================================================
# Keep this transport identical to tools/test_live_mic.py:
# sounddevice -> 16 kHz PCM -> Gemini Live. Gemini owns turn detection.

async def microphone_sender(session):
    global listen_ready, model_speaking
    loop = asyncio.get_running_loop()
    audio_queue = asyncio.Queue(maxsize=80)

    def enqueue(data):
        if shutdown_requested:
            return
        try:
            audio_queue.put_nowait(data)
        except asyncio.QueueFull:
            try:
                audio_queue.get_nowait()
            except asyncio.QueueEmpty:
                pass
            try:
                audio_queue.put_nowait(data)
            except asyncio.QueueFull:
                pass

    def callback(indata, frames, time_info, status):
        if status:
            print("MIC:", status, flush=True)
        try:
            loop.call_soon_threadsafe(enqueue, bytes(indata))
        except Exception as e:
            print("⚠️ MIC callback lỗi:", repr(e), flush=True)

    device_info = sd.query_devices(MIC, "input")
    print(f"🎙️ MIC device {MIC}: {device_info['name']}", flush=True)
    print(f"🎙️ MIC channels={CHANNELS} rate={INPUT_RATE} block={BLOCKSIZE}", flush=True)

    with sd.RawInputStream(
        device=MIC,
        samplerate=INPUT_RATE,
        channels=CHANNELS,
        dtype="int16",
        blocksize=BLOCKSIZE,
        callback=callback,
    ):
        print("🎤 Microphone sẵn sàng.", flush=True)
        print("⏳ Chờ Tiểu Vũ nói xong rồi bắt đầu nghe.", flush=True)
        sent = 0
        while not shutdown_requested:
            data = await audio_queue.get()
            if not listen_ready or model_speaking:
                continue
            try:
                await session.send_realtime_input(
                    audio=types.Blob(data=data, mime_type="audio/pcm;rate=16000")
                )
                sent += len(data)
            except Exception as e:
                print("⚠️ MIC gửi audio lỗi:", repr(e), flush=True)
                continue
            if sent >= 32000:
                print(f"🎤 MIC → Gemini: {sent} bytes", flush=True)
                sent = 0

'''
          s = s[:start] + microphone + s[end:]

          old = '''        # Automatic VAD is deliberately disabled. microphone_sender owns the
        # activity_start/activity_end boundaries.
        realtime_input_config=types.RealtimeInputConfig(
            automatic_activity_detection=types.AutomaticActivityDetection(disabled=True)
        ),'''
          new = '''        # Same turn-detection path proven by tools/test_live_mic.py.
        realtime_input_config=types.RealtimeInputConfig(
            automatic_activity_detection=types.AutomaticActivityDetection(
                disabled=False,
                start_of_speech_sensitivity=types.StartSensitivity.START_SENSITIVITY_HIGH,
                end_of_speech_sensitivity=types.EndSensitivity.END_SENSITIVITY_LOW,
                prefix_padding_ms=100,
                silence_duration_ms=500,
            )
        ),'''
          if old not in s:
              raise SystemExit("Disabled VAD config not found; refusing to modify config")
          s = s.replace(old, new, 1)
          p.write_text(s, encoding="utf-8")
          PY

      - name: Verify Python syntax
        run: python -m py_compile live_voice.py

      - name: Commit only live_voice.py and remove this workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add live_voice.py
          git rm .github/workflows/rebuild-live-input.yml
          git commit -m "Rebuild Live Voice input transport"
          git push